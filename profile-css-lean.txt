/* ==========================================================================
   THE SERAPH'S CRYPT — SITE-WIDE STYLING  (working set)
   ==========================================================================
   PASTE THIS INTO:  JanitorAI -> CSS Editor

   CSS only, no <style> tag — correct for the CSS Editor.
   Do NOT paste this into About Me on its own; it would show as text.

   Only selectors that genuinely match the live site.
   ========================================================================== */

/* ==========================================================================
   THE SERAPH'S CRYPT — WORKING CSS
   Only what bites on JanitorAI: no hashed class names, no guesswork.
   ========================================================================== */

/* THE STARLESS SKY — every layer of the page painted out */
html, body, #root, #__next, main,
[class*="App"], [class*="app"],
[class*="Layout"], [class*="layout"],
[class*="Page"], [class*="page"],
[class*="Container"], [class*="container"],
[class*="Wrapper"], [class*="wrapper"],
[class*="Background"], [class*="background"],
[class*="Profile"], [class*="profile"],
[class*="Content"], [class*="content"] {
  background-color: #000000 !important;
  background-image: none !important;
}
body { color: #dedbe1 !important; }
body {
  background-color: #000000 !important;
  background-image:
    linear-gradient(rgba(0, 0, 0, 0.84), rgba(0, 0, 0, 0.93)),
    url("https://belovedangel02.github.io/darts-of-doom/seraphs-crypt/images/crypt-background.jpg") !important;
  background-repeat: no-repeat, no-repeat !important;
  background-position: center top, center top !important;
  background-size: cover, cover !important;
  background-attachment: fixed, fixed !important;
}

/* THE SEALS THAT OPEN — the angel-wing sections */
summary { list-style: none !important; cursor: pointer;
  transition: border-color 0.3s ease, background-color 0.3s ease !important; }
summary::-webkit-details-marker { display: none !important; }
summary::marker { content: "" !important; }
summary:hover { border-color: rgba(150, 30, 45, 0.8) !important; background-color: #1a121e !important; }
summary span:last-child { transition: transform 0.4s ease, box-shadow 0.4s ease !important; }
details[open] > summary span:last-child {
  transform: rotate(-12deg) scale(1.08) !important;
  box-shadow: 0 0 12px rgba(112, 14, 26, 0.55) !important;
}

/* THE TURNING RECORD — the SoundCloud disc */
@keyframes cryptSpin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
[class*="udioPlayer"] img, [class*="usicPlayer"] img,
[class*="oundCloud"] img, [class*="oundcloud"] img,
[class*="albumArt"], [class*="AlbumArt"],
[class*="artwork"], [class*="Artwork"],
.crypt-disc {
  border-radius: 50% !important;
  border: 1px solid rgba(197, 184, 202, 0.45) !important;
  box-shadow: 0 0 16px rgba(112, 14, 26, 0.3) !important;
  animation: cryptSpin 8s linear infinite !important;
}
[class*="udioPlayer"], [class*="usicPlayer"],
[class*="oundCloud"], [class*="oundcloud"] {
  background-color: #070608 !important;
  border: 1px solid rgba(112, 14, 26, 0.55) !important;
  border-radius: 18px !important;
  color: #c5b8ca !important;
}


/* The disc is the player now — the iframe never survived the sanitiser. */
.crypt-disc-link { transition: transform 0.4s ease; }
.crypt-disc-link:hover, .crypt-disc-link:focus-visible { transform: scale(1.05); outline: none; }
.crypt-disc-link:focus-visible .crypt-disc { box-shadow: 0 0 0 2px #c5b8ca !important; }
.crypt-disc-link:hover span:last-child { color: #c5b8ca !important; }

/* THE GLASS OF REVELATION — the live character search */
input[type="text"], input[type="search"], input[placeholder*="earch"] {
  background-color: #050505 !important;
  color: #dedbe1 !important;
  border: 1px solid rgba(112, 14, 26, 0.5) !important;
  border-radius: 30px !important;
  padding: 10px 20px !important;
  font-family: Georgia, 'Times New Roman', serif !important;
  letter-spacing: 1px !important;
}
input[placeholder*="earch"]::placeholder { color: #5c5860 !important; font-style: italic !important; }
input[type="text"]:focus, input[type="search"]:focus {
  outline: none !important;
  border-color: #c5b8ca !important;
  box-shadow: 0 0 12px rgba(197, 184, 202, 0.25) !important;
}
select {
  background-color: #050505 !important;
  color: #c5b8ca !important;
  border: 1px solid rgba(112, 14, 26, 0.5) !important;
  border-radius: 30px !important;
  font-family: Georgia, 'Times New Roman', serif !important;
}

/* THE STONE MARKERS — pagination */
[aria-current="page"] {
  background-color: #302a36 !important;
  color: #dedbe1 !important;
  border-color: #c5b8ca !important;
  box-shadow: 0 0 12px rgba(112, 14, 26, 0.4) !important;
}

/* THE WAKING OF THE PORTRAIT — cards sleep grey, wake in colour */
a:has(img) img { filter: grayscale(100%) contrast(108%) brightness(0.92);
  transition: filter 0.55s ease, transform 0.55s ease; }
a:has(img):hover img, a:has(img):active img, a:has(img):focus-within img {
  filter: grayscale(0%) contrast(105%) brightness(1); transform: scale(1.03); }
#crypt-bio img, #crypt-bio a img { filter: none !important; transform: none !important; }

/* THE WINDING PASSAGE — scrollbar */
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: #050505; }
::-webkit-scrollbar-thumb { background: #4c4751; border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: #c5b8ca; }
* { scrollbar-width: thin; scrollbar-color: #4c4751 #050505; }

/* ==========================================================================
   THE VESSELS — the bot cards
   Selector names verified against Hime's freely-published Dark Red template.
   See JANITOR-SELECTORS.md. Written in the crypt's own palette; no borrowed
   declarations.
   ========================================================================== */

/* The plate. Name across the head, portrait held on the left, the writing
   and its tags to the right, the world named along the foot. */
.profile-character-card-stack {
  display: grid !important;
  grid-template-columns: minmax(0, 1fr) minmax(0, 1.15fr);
  grid-template-rows: auto auto 1fr auto;
  grid-template-areas:
    "name  name"
    "pfp   desc"
    "pfp   tags"
    "world world";
  background: linear-gradient(-95deg, #050505, #0d0509) !important;
  border: 1px solid rgba(112, 14, 26, 0.5) !important;
  border-radius: 14px;
  overflow: hidden;
  transition: border-color 0.4s ease;
}
.pp-cc-wrapper:hover .profile-character-card-stack {
  border-color: rgba(150, 30, 45, 0.9) !important;
}

.profile-character-card-stack-link-component-box { grid-area: name; }
.pp-cc-name {
  padding: 7px 12px;
  background: linear-gradient(to right, rgba(112, 14, 26, 0.55), transparent) !important;
  color: #dedbe1 !important;
  font-family: Georgia, 'Times New Roman', serif !important;
  font-size: 15px !important;
  letter-spacing: 2px;
}

.profile-character-card-avatar-aspect-ratio { grid-area: pfp; height: 250px; }
.pp-cc-avatar { padding: 8px; border-radius: 12px; background: #030303; }

.profile-character-card-description-box { grid-area: desc; padding: 4px 12px 0 4px; }
.pp-cc-description, .pp-cc-description * {
  color: #8c8791 !important;
  background: transparent !important;
  border: none !important;
  box-shadow: none !important;
  font-size: 12px !important;
  line-height: 1.75 !important;
}
/* A dropped capital, the way the museum labels open. */
.pp-cc-description::first-letter {
  color: #c5b8ca;
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 24px;
  line-height: 0.8;
}

.pp-cc-tags {
  grid-area: tags;
  align-self: end;
  width: 100%;
  padding: 0 12px 8px 4px;
  overflow-x: auto;
  overflow-y: hidden;
}
.chakra-wrap__list { display: flex; flex-wrap: nowrap; }
.chakra-wrap__listitem { flex-shrink: 0; margin: 0; }
.pp-cc-tags-wrap span {
  display: inline-block;
  margin: 0 3px 0 0;
  padding: 2px 10px;
  background: #070608 !important;
  border: 1px solid rgba(197, 184, 202, 0.18) !important;
  border-radius: 20px;
  color: #978a9e !important;
  font-size: 10px !important;
  letter-spacing: 1px;
  transition: border-color 0.3s ease, color 0.3s ease;
}
.pp-cc-tags-wrap span:hover {
  border-color: rgba(112, 14, 26, 0.8) !important;
  color: #dedbe1 !important;
}

/* Things the crypt does not need on a plate. */
.pp-cc-creator-name, .pp-cc-star-line, .pp-cc-description hr { display: none !important; }

/* THE WORLD, NAMED — each card reads its own tags and stamps itself.
   -----------------------------------------------------------------
   A tag becomes a class: lowercased, spaces and punctuation dropped.
   "Dead Dove" -> .pp-tag-deaddove, "Fourth Wing" -> .pp-tag-fourthwing.
   Correct the eight below to whatever you actually tag the bots with —
   right-click a tag on your profile, Inspect, and read the class. */
.profile-character-card-stack::after {
  content: 'THE CRYPT';
  grid-area: world;
  padding: 3px 12px;
  background: linear-gradient(to left, rgba(112, 14, 26, 0.55), transparent);
  color: #c5b8ca;
  font-family: Georgia, 'Times New Roman', serif;
  font-size: 10px;
  letter-spacing: 3px;
  text-align: right;
}
.pp-cc-wrapper:has(.pp-tag-90210) .profile-character-card-stack::after { content: '90210'; }
.pp-cc-wrapper:has(.pp-tag-fourthwing) .profile-character-card-stack::after { content: 'FOURTH WING'; }
.pp-cc-wrapper:has(.pp-tag-supernatural) .profile-character-card-stack::after { content: 'SUPERNATURAL'; }
.pp-cc-wrapper:has(.pp-tag-percyjackson) .profile-character-card-stack::after { content: 'CAMP HALF-BLOOD'; }
.pp-cc-wrapper:has(.pp-tag-elfhame) .profile-character-card-stack::after { content: 'ELFHAME'; }
.pp-cc-wrapper:has(.pp-tag-anahuang) .profile-character-card-stack::after { content: 'ANA HUANG'; }
.pp-cc-wrapper:has(.pp-tag-dystopian) .profile-character-card-stack::after { content: 'DARK ROMANCE & DYSTOPIAN'; }
.pp-cc-wrapper:has(.pp-tag-losttales) .profile-character-card-stack::after { content: 'THE LOST TALES'; }

/* THE TALLY — the heading above the vessels, and the count beneath it */
#profile-tabs::before {
  content: "THE VESSELS";
  display: block;
  width: 100%;
  margin-top: 2.5rem;
  padding: 10px;
  color: #dedbe1;
  font-family: Georgia, 'Times New Roman', serif;
  font-size: clamp(2.4rem, 7vw, 4.5rem);
  letter-spacing: 8px;
  line-height: 1;
  text-align: center;
  pointer-events: none;
}
.pp-tabs-button, .pp-tabs-indicator { display: none !important; }
.Btn2-purple strong {
  color: #c5b8ca !important;
  font-family: Georgia, 'Times New Roman', serif !important;
  font-size: clamp(1.6rem, 4vw, 2.2rem) !important;
}

/* THE WORDMARK — the crypt names itself in the site's own corner */
.glow-logo :is(h2, p) {
  font-size: 0 !important;
  color: #c5b8ca !important;
  font-family: Georgia, 'Times New Roman', serif !important;
  text-shadow: none !important;
}
.glow-logo h2::after { content: "THE SERAPH\2019S"; font-size: 17px; letter-spacing: 3px; }
.glow-logo p::after  { content: " CRYPT"; font-size: 17px; letter-spacing: 3px; color: #700e1a; }

/* THE COUNT — followers, told as the crypt would tell it */
.pp-uc-followers-count {
  color: #c5b8ca !important;
  font-family: Georgia, 'Times New Roman', serif !important;
  font-weight: normal !important;
}

/* Narrow screens: the plate stops being two columns and stacks. */
@media (max-width: 560px) {
  .profile-character-card-stack {
    grid-template-columns: 100%;
    grid-template-areas: "name" "pfp" "desc" "tags" "world";
  }
  .profile-character-card-avatar-aspect-ratio { height: auto; }
}

/* MERCY FOR THOSE WHO ASK FOR STILLNESS */
@media (prefers-reduced-motion: reduce) {
  .crypt-disc, [class*="albumArt"], [class*="AlbumArt"],
  [class*="udioPlayer"] img, [class*="usicPlayer"] img { animation: none !important; }
  .profile-character-card-stack, .pp-cc-tags-wrap span { transition: none !important; }
}
